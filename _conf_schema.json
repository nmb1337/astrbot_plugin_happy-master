{
    "group_whitelist": {
        "type": "list",
        "description": "群白名单（群号），只有白名单内的群才会启用反撤回。留空表示所有群都启用。",
        "default": [],
        "required": false
    },
    "whitelist": {
        "type": "list",
        "description": "反撤回用户白名单（QQ号），白名单内用户撤回消息不会被复读。群主和管理员默认豁免，无需添加。",
        "default": [],
        "required": false
    }
}
